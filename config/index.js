import { base_config } from './base.js';
import { mysql_config } from './db.js';
import { redis_config } from './cache.js';

export const config = {
    base_config,
    mysql_config,
    redis_config,
};